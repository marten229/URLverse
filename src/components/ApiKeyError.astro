---
interface Props {
    error: string;
}

const { error } = Astro.props;
---

<div class="api-key-error-container">
    <div class="error-content">
        <div class="icon">🔑</div>
        <h1 class="error-title">API Key erforderlich</h1>
        <p class="error-subtitle">
            Um das URLverse zu erkunden, benötigen wir deinen Schlüssel zum
            Universum.
        </p>

        <div class="error-card">
            <p class="error-message">
                {error}
            </p>
            <p class="error-hint">
                Setzen Sie Ihren persönlichen Gemini API Key in den
                Einstellungen, um generierte Inhalte zu sehen.
            </p>

            <div class="action-buttons">
                <a href="/" class="home-button">🏠 Zur Startseite</a>
            </div>
        </div>

        <details class="help-section">
            <summary class="help-summary"
                >Wie bekomme ich einen API Key?</summary
            >
            <ol class="help-list">
                <li>
                    Gehen Sie zu <a
                        href="https://aistudio.google.com/app/apikey"
                        target="_blank">Google AI Studio</a
                    >
                </li>
                <li>Melden Sie sich mit Ihrem Google-Konto an</li>
                <li>Klicken Sie auf "Create API Key"</li>
                <li>Kopieren Sie den generierten Key</li>
                <li>Öffnen Sie die Einstellungen in URLverse</li>
                <li>Fügen Sie Ihren API Key ein und speichern Sie ihn</li>
            </ol>
        </details>
    </div>
</div>

<style>
    .api-key-error-container {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #ffffff;
        padding: 2rem;
        font-family:
            -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
            "Helvetica Neue", Arial, sans-serif;
    }

    .error-content {
        max-width: 600px;
        width: 100%;
        text-align: center;
        animation: fadeIn 0.3s ease-in-out;
    }

    .icon {
        font-size: 4rem;
        margin-bottom: 1.5rem;
        text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        display: block;
        animation: float 3s ease-in-out infinite;
    }

    .error-title {
        font-size: 2.25rem;
        font-weight: 700;
        margin: 0 0 1rem 0;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        color: #ffffff;
    }

    .error-subtitle {
        font-size: 1.125rem;
        margin-bottom: 2rem;
        opacity: 0.9;
        line-height: 1.625;
    }

    .error-card {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 0.75rem;
        padding: 2rem;
        margin-bottom: 1.5rem;
        box-shadow:
            0 20px 25px -5px rgba(0, 0, 0, 0.1),
            0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }

    .error-message {
        background: rgba(231, 76, 60, 0.25);
        color: #ffcccc;
        border: 1px solid rgba(231, 76, 60, 0.5);
        padding: 1rem;
        border-radius: 0.375rem;
        margin-bottom: 1.5rem;
        font-family: "SF Mono", Monaco, Consolas, "Courier New", monospace;
        font-size: 0.875rem;
        text-align: left;
    }

    .error-hint {
        margin-bottom: 2rem;
        line-height: 1.625;
        color: rgba(255, 255, 255, 0.9);
    }

    .action-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .home-button {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.5rem;
        background: rgba(255, 255, 255, 0.2);
        color: #ffffff;
        text-decoration: none;
        border-radius: 9999px;
        font-weight: 600;
        transition: all 0.15s ease-in-out;
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .home-button:hover {
        background: rgba(255, 255, 255, 0.3);
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    }

    .help-section {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 0.5rem;
        padding: 1.5rem;
        text-align: left;
        color: #ffffff;
    }

    .help-summary {
        cursor: pointer;
        font-weight: 600;
        color: #ffd700;
        user-select: none;
        list-style: none;
    }

    .help-summary::-webkit-details-marker {
        display: none;
    }

    .help-summary::before {
        content: "▶ ";
        font-size: 0.75rem;
    }

    details[open] .help-summary::before {
        content: "▼ ";
    }

    .help-section[open] .help-summary {
        margin-bottom: 1rem;
    }

    .help-list {
        margin: 1rem 0 0 0;
        padding-left: 1.5rem;
        color: rgba(255, 255, 255, 0.85);
        line-height: 1.625;
    }

    .help-list li {
        margin-bottom: 0.25rem;
    }

    .help-list a {
        color: #ffd700;
        text-decoration: none;
        border-bottom: 1px dashed #ffd700;
    }

    .help-list a:hover {
        border-bottom-style: solid;
    }

    @keyframes float {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-10px);
        }
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
